import { BeautyContatoPipe } from './beauty-contato.pipe';

describe('BeautyContatoPipe', () => {
  it('create an instance', () => {
    const pipe = new BeautyContatoPipe();
    expect(pipe).toBeTruthy();
  });
});
