import { FormatDataDirective } from './format-data.directive';

describe('FormatDataDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatDataDirective();
    expect(directive).toBeTruthy();
  });
});
