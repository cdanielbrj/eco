import { FormatHoraDirective } from './format-hora.directive';

describe('FormatHoraDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatHoraDirective();
    expect(directive).toBeTruthy();
  });
});
