import { FormatContatoDirective } from './format-contato.directive';

describe('FormatContatoDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatContatoDirective();
    expect(directive).toBeTruthy();
  });
});
